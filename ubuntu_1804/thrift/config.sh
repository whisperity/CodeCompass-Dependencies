#!/bin/bash

export DEPLOY_ROOT="${HOME}/deploy"

export THRIFT_VERSION="0.13.0"
export THRIFT_FILE="thrift-${THRIFT_VERSION}.tar.gz"
export THRIFT_UNPACK_DIR="thrift-${THRIFT_VERSION}"

export THRIFT_SRC="${HOME}/thrift-src"
export THRIFT_INSTALL="${HOME}/thrift-install"
